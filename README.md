# 拳头减肥法

一个公开静态 PWA。规则来自用户提供的截图：按体重档位分配每天拳头数，并用 `1 拳头约 150 卡或 100 克` 做粗略记录。

## 功能

- 今日打卡：输入近期目标体重，自动匹配体重档位并计算一天吃几拳，记录早餐、午餐、晚餐、加餐的拳头数。
- 拳头规则：展示 90-130 斤对应拳头数。
- 记录统计：查看打卡天数、达标天数、累计拳头和最近记录。

数据只保存在当前浏览器 `localStorage`，没有账号、服务器或第三方接口。
